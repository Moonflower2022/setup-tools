alias cl=clear
alias mkd=mkdir
alias mk=touch
alias rmd="rm -rf"
alias sudo="sudo "
alias ll="ls -al"

PS1="%m %1~ %# "

export EDITOR=nano # for some reason the default editor for crontab is vim and im scared of vim